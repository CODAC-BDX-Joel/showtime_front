import React from 'react';
import AdminOptions from "./components/AdminOptions";

const AdminUsers = () => {
    return (
        <div>
            <AdminOptions/>
            <button>Add User</button>
            <p>Users list will be there</p>
        </div>
    );
};

export default AdminUsers;